# Azure Functions runtime
azure-functions==1.21.3

# Storage (enriched_data.json from Blob)
azure-storage-blob==12.19.0

# Cosmos DB (user profiles, favorites, history)
azure-cosmos==4.7.0

# Fuzzy title matching
rapidfuzz==3.9.6

# HTTP calls (Google Books, Azure OpenAI, Translator, Vision)
requests==2.32.3

# JWT validation for Entra External ID tokens
PyJWT==2.8.0
cryptography==42.0.8

# Azure AI Translation (kept for reference)
azure-ai-translation-text==1.0.0

# Azure OpenAI SDK
openai>=1.93.0
